// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.1
// source: library/v1/library.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "api.library.v1";

export enum ReadingStatus {
  READING_STATUS_UNSPECIFIED = 0,
  READING_STATUS_TO_READ = 1,
  READING_STATUS_READING = 2,
  READING_STATUS_READ = 3,
  READING_STATUS_ABANDONED = 4,
  UNRECOGNIZED = -1,
}

export function readingStatusFromJSON(object: any): ReadingStatus {
  switch (object) {
    case 0:
    case "READING_STATUS_UNSPECIFIED":
      return ReadingStatus.READING_STATUS_UNSPECIFIED;
    case 1:
    case "READING_STATUS_TO_READ":
      return ReadingStatus.READING_STATUS_TO_READ;
    case 2:
    case "READING_STATUS_READING":
      return ReadingStatus.READING_STATUS_READING;
    case 3:
    case "READING_STATUS_READ":
      return ReadingStatus.READING_STATUS_READ;
    case 4:
    case "READING_STATUS_ABANDONED":
      return ReadingStatus.READING_STATUS_ABANDONED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReadingStatus.UNRECOGNIZED;
  }
}

export function readingStatusToJSON(object: ReadingStatus): string {
  switch (object) {
    case ReadingStatus.READING_STATUS_UNSPECIFIED:
      return "READING_STATUS_UNSPECIFIED";
    case ReadingStatus.READING_STATUS_TO_READ:
      return "READING_STATUS_TO_READ";
    case ReadingStatus.READING_STATUS_READING:
      return "READING_STATUS_READING";
    case ReadingStatus.READING_STATUS_READ:
      return "READING_STATUS_READ";
    case ReadingStatus.READING_STATUS_ABANDONED:
      return "READING_STATUS_ABANDONED";
    case ReadingStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Library {
  id: number;
  ownerId: number;
  name: string;
  description?: string | undefined;
  isPublic: boolean;
  articles: LibraryArticle[];
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
}

export interface LibraryArticle {
  id: number;
  articleId: number;
  readingStatus: ReadingStatus;
  readingProgress: number;
  dateAdded: Date | undefined;
  dateCompleted: Date | undefined;
  notes?: string | undefined;
  articleTitle: string;
  doi: string;
  publicationYear: number;
  isFavorite: boolean;
}

export interface SaveArticleToLibraryRequest {
  libraryId: number;
  articleId: number;
  readingStatus: ReadingStatus;
  notes?: string | undefined;
}

export interface SaveArticleToLibraryResponse {
  id: number;
}

export interface GetUserLibraryRequest {
  userId: number;
}

export interface GetUserLibraryResponse {
  defaultLibrary: Library | undefined;
  privateLibraries: Library[];
}

export interface GetLibraryRequest {
  libraryId: number;
}

export interface GetLibraryResponse {
  library: Library | undefined;
}

export interface CreateLibraryRequest {
  ownerId: number;
  name: string;
  description?: string | undefined;
  isPublic: boolean;
}

export interface CreateLibraryResponse {
  libraryId: number;
}

export interface UpdateLibraryRequest {
  libraryId: number;
  name?: string | undefined;
  description?: string | undefined;
  isPublic?: boolean | undefined;
}

export interface UpdateLibraryResponse {
  success: boolean;
}

export interface DeleteLibraryRequest {
  libraryId: number;
}

export interface DeleteLibraryResponse {
  success: boolean;
}

function createBaseLibrary(): Library {
  return {
    id: 0,
    ownerId: 0,
    name: "",
    description: undefined,
    isPublic: false,
    articles: [],
    createdAt: undefined,
    updatedAt: undefined,
  };
}

export const Library: MessageFns<Library> = {
  encode(message: Library, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.ownerId !== 0) {
      writer.uint32(16).int64(message.ownerId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(42).string(message.description);
    }
    if (message.isPublic !== false) {
      writer.uint32(48).bool(message.isPublic);
    }
    for (const v of message.articles) {
      LibraryArticle.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(66).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Library {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLibrary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ownerId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.articles.push(LibraryArticle.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Library {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      ownerId: isSet(object.ownerId) ? globalThis.Number(object.ownerId) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      articles: globalThis.Array.isArray(object?.articles)
        ? object.articles.map((e: any) => LibraryArticle.fromJSON(e))
        : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
    };
  },

  toJSON(message: Library): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.ownerId !== 0) {
      obj.ownerId = Math.round(message.ownerId);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.articles?.length) {
      obj.articles = message.articles.map((e) => LibraryArticle.toJSON(e));
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Library>, I>>(base?: I): Library {
    return Library.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Library>, I>>(object: I): Library {
    const message = createBaseLibrary();
    message.id = object.id ?? 0;
    message.ownerId = object.ownerId ?? 0;
    message.name = object.name ?? "";
    message.description = object.description ?? undefined;
    message.isPublic = object.isPublic ?? false;
    message.articles = object.articles?.map((e) => LibraryArticle.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseLibraryArticle(): LibraryArticle {
  return {
    id: 0,
    articleId: 0,
    readingStatus: 0,
    readingProgress: 0,
    dateAdded: undefined,
    dateCompleted: undefined,
    notes: undefined,
    articleTitle: "",
    doi: "",
    publicationYear: 0,
    isFavorite: false,
  };
}

export const LibraryArticle: MessageFns<LibraryArticle> = {
  encode(message: LibraryArticle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.articleId !== 0) {
      writer.uint32(16).int64(message.articleId);
    }
    if (message.readingStatus !== 0) {
      writer.uint32(24).int32(message.readingStatus);
    }
    if (message.readingProgress !== 0) {
      writer.uint32(32).int32(message.readingProgress);
    }
    if (message.dateAdded !== undefined) {
      Timestamp.encode(toTimestamp(message.dateAdded), writer.uint32(42).fork()).join();
    }
    if (message.dateCompleted !== undefined) {
      Timestamp.encode(toTimestamp(message.dateCompleted), writer.uint32(50).fork()).join();
    }
    if (message.notes !== undefined) {
      writer.uint32(58).string(message.notes);
    }
    if (message.articleTitle !== "") {
      writer.uint32(66).string(message.articleTitle);
    }
    if (message.doi !== "") {
      writer.uint32(74).string(message.doi);
    }
    if (message.publicationYear !== 0) {
      writer.uint32(80).int32(message.publicationYear);
    }
    if (message.isFavorite !== false) {
      writer.uint32(88).bool(message.isFavorite);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LibraryArticle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLibraryArticle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.articleId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.readingStatus = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.readingProgress = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dateAdded = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dateCompleted = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.articleTitle = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.doi = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.publicationYear = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.isFavorite = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LibraryArticle {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      articleId: isSet(object.articleId) ? globalThis.Number(object.articleId) : 0,
      readingStatus: isSet(object.readingStatus) ? readingStatusFromJSON(object.readingStatus) : 0,
      readingProgress: isSet(object.readingProgress) ? globalThis.Number(object.readingProgress) : 0,
      dateAdded: isSet(object.dateAdded) ? fromJsonTimestamp(object.dateAdded) : undefined,
      dateCompleted: isSet(object.dateCompleted) ? fromJsonTimestamp(object.dateCompleted) : undefined,
      notes: isSet(object.notes) ? globalThis.String(object.notes) : undefined,
      articleTitle: isSet(object.articleTitle) ? globalThis.String(object.articleTitle) : "",
      doi: isSet(object.doi) ? globalThis.String(object.doi) : "",
      publicationYear: isSet(object.publicationYear) ? globalThis.Number(object.publicationYear) : 0,
      isFavorite: isSet(object.isFavorite) ? globalThis.Boolean(object.isFavorite) : false,
    };
  },

  toJSON(message: LibraryArticle): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.articleId !== 0) {
      obj.articleId = Math.round(message.articleId);
    }
    if (message.readingStatus !== 0) {
      obj.readingStatus = readingStatusToJSON(message.readingStatus);
    }
    if (message.readingProgress !== 0) {
      obj.readingProgress = Math.round(message.readingProgress);
    }
    if (message.dateAdded !== undefined) {
      obj.dateAdded = message.dateAdded.toISOString();
    }
    if (message.dateCompleted !== undefined) {
      obj.dateCompleted = message.dateCompleted.toISOString();
    }
    if (message.notes !== undefined) {
      obj.notes = message.notes;
    }
    if (message.articleTitle !== "") {
      obj.articleTitle = message.articleTitle;
    }
    if (message.doi !== "") {
      obj.doi = message.doi;
    }
    if (message.publicationYear !== 0) {
      obj.publicationYear = Math.round(message.publicationYear);
    }
    if (message.isFavorite !== false) {
      obj.isFavorite = message.isFavorite;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LibraryArticle>, I>>(base?: I): LibraryArticle {
    return LibraryArticle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LibraryArticle>, I>>(object: I): LibraryArticle {
    const message = createBaseLibraryArticle();
    message.id = object.id ?? 0;
    message.articleId = object.articleId ?? 0;
    message.readingStatus = object.readingStatus ?? 0;
    message.readingProgress = object.readingProgress ?? 0;
    message.dateAdded = object.dateAdded ?? undefined;
    message.dateCompleted = object.dateCompleted ?? undefined;
    message.notes = object.notes ?? undefined;
    message.articleTitle = object.articleTitle ?? "";
    message.doi = object.doi ?? "";
    message.publicationYear = object.publicationYear ?? 0;
    message.isFavorite = object.isFavorite ?? false;
    return message;
  },
};

function createBaseSaveArticleToLibraryRequest(): SaveArticleToLibraryRequest {
  return { libraryId: 0, articleId: 0, readingStatus: 0, notes: undefined };
}

export const SaveArticleToLibraryRequest: MessageFns<SaveArticleToLibraryRequest> = {
  encode(message: SaveArticleToLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.libraryId !== 0) {
      writer.uint32(8).int64(message.libraryId);
    }
    if (message.articleId !== 0) {
      writer.uint32(16).int64(message.articleId);
    }
    if (message.readingStatus !== 0) {
      writer.uint32(24).int32(message.readingStatus);
    }
    if (message.notes !== undefined) {
      writer.uint32(34).string(message.notes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveArticleToLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveArticleToLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.libraryId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.articleId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.readingStatus = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveArticleToLibraryRequest {
    return {
      libraryId: isSet(object.libraryId) ? globalThis.Number(object.libraryId) : 0,
      articleId: isSet(object.articleId) ? globalThis.Number(object.articleId) : 0,
      readingStatus: isSet(object.readingStatus) ? readingStatusFromJSON(object.readingStatus) : 0,
      notes: isSet(object.notes) ? globalThis.String(object.notes) : undefined,
    };
  },

  toJSON(message: SaveArticleToLibraryRequest): unknown {
    const obj: any = {};
    if (message.libraryId !== 0) {
      obj.libraryId = Math.round(message.libraryId);
    }
    if (message.articleId !== 0) {
      obj.articleId = Math.round(message.articleId);
    }
    if (message.readingStatus !== 0) {
      obj.readingStatus = readingStatusToJSON(message.readingStatus);
    }
    if (message.notes !== undefined) {
      obj.notes = message.notes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SaveArticleToLibraryRequest>, I>>(base?: I): SaveArticleToLibraryRequest {
    return SaveArticleToLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SaveArticleToLibraryRequest>, I>>(object: I): SaveArticleToLibraryRequest {
    const message = createBaseSaveArticleToLibraryRequest();
    message.libraryId = object.libraryId ?? 0;
    message.articleId = object.articleId ?? 0;
    message.readingStatus = object.readingStatus ?? 0;
    message.notes = object.notes ?? undefined;
    return message;
  },
};

function createBaseSaveArticleToLibraryResponse(): SaveArticleToLibraryResponse {
  return { id: 0 };
}

export const SaveArticleToLibraryResponse: MessageFns<SaveArticleToLibraryResponse> = {
  encode(message: SaveArticleToLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveArticleToLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveArticleToLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaveArticleToLibraryResponse {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: SaveArticleToLibraryResponse): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SaveArticleToLibraryResponse>, I>>(base?: I): SaveArticleToLibraryResponse {
    return SaveArticleToLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SaveArticleToLibraryResponse>, I>>(object: I): SaveArticleToLibraryResponse {
    const message = createBaseSaveArticleToLibraryResponse();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGetUserLibraryRequest(): GetUserLibraryRequest {
  return { userId: 0 };
}

export const GetUserLibraryRequest: MessageFns<GetUserLibraryRequest> = {
  encode(message: GetUserLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserLibraryRequest {
    return { userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0 };
  },

  toJSON(message: GetUserLibraryRequest): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserLibraryRequest>, I>>(base?: I): GetUserLibraryRequest {
    return GetUserLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserLibraryRequest>, I>>(object: I): GetUserLibraryRequest {
    const message = createBaseGetUserLibraryRequest();
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseGetUserLibraryResponse(): GetUserLibraryResponse {
  return { defaultLibrary: undefined, privateLibraries: [] };
}

export const GetUserLibraryResponse: MessageFns<GetUserLibraryResponse> = {
  encode(message: GetUserLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.defaultLibrary !== undefined) {
      Library.encode(message.defaultLibrary, writer.uint32(10).fork()).join();
    }
    for (const v of message.privateLibraries) {
      Library.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.defaultLibrary = Library.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.privateLibraries.push(Library.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserLibraryResponse {
    return {
      defaultLibrary: isSet(object.defaultLibrary) ? Library.fromJSON(object.defaultLibrary) : undefined,
      privateLibraries: globalThis.Array.isArray(object?.privateLibraries)
        ? object.privateLibraries.map((e: any) => Library.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUserLibraryResponse): unknown {
    const obj: any = {};
    if (message.defaultLibrary !== undefined) {
      obj.defaultLibrary = Library.toJSON(message.defaultLibrary);
    }
    if (message.privateLibraries?.length) {
      obj.privateLibraries = message.privateLibraries.map((e) => Library.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserLibraryResponse>, I>>(base?: I): GetUserLibraryResponse {
    return GetUserLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserLibraryResponse>, I>>(object: I): GetUserLibraryResponse {
    const message = createBaseGetUserLibraryResponse();
    message.defaultLibrary = (object.defaultLibrary !== undefined && object.defaultLibrary !== null)
      ? Library.fromPartial(object.defaultLibrary)
      : undefined;
    message.privateLibraries = object.privateLibraries?.map((e) => Library.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetLibraryRequest(): GetLibraryRequest {
  return { libraryId: 0 };
}

export const GetLibraryRequest: MessageFns<GetLibraryRequest> = {
  encode(message: GetLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.libraryId !== 0) {
      writer.uint32(8).int64(message.libraryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.libraryId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLibraryRequest {
    return { libraryId: isSet(object.libraryId) ? globalThis.Number(object.libraryId) : 0 };
  },

  toJSON(message: GetLibraryRequest): unknown {
    const obj: any = {};
    if (message.libraryId !== 0) {
      obj.libraryId = Math.round(message.libraryId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLibraryRequest>, I>>(base?: I): GetLibraryRequest {
    return GetLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLibraryRequest>, I>>(object: I): GetLibraryRequest {
    const message = createBaseGetLibraryRequest();
    message.libraryId = object.libraryId ?? 0;
    return message;
  },
};

function createBaseGetLibraryResponse(): GetLibraryResponse {
  return { library: undefined };
}

export const GetLibraryResponse: MessageFns<GetLibraryResponse> = {
  encode(message: GetLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.library !== undefined) {
      Library.encode(message.library, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.library = Library.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLibraryResponse {
    return { library: isSet(object.library) ? Library.fromJSON(object.library) : undefined };
  },

  toJSON(message: GetLibraryResponse): unknown {
    const obj: any = {};
    if (message.library !== undefined) {
      obj.library = Library.toJSON(message.library);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLibraryResponse>, I>>(base?: I): GetLibraryResponse {
    return GetLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLibraryResponse>, I>>(object: I): GetLibraryResponse {
    const message = createBaseGetLibraryResponse();
    message.library = (object.library !== undefined && object.library !== null)
      ? Library.fromPartial(object.library)
      : undefined;
    return message;
  },
};

function createBaseCreateLibraryRequest(): CreateLibraryRequest {
  return { ownerId: 0, name: "", description: undefined, isPublic: false };
}

export const CreateLibraryRequest: MessageFns<CreateLibraryRequest> = {
  encode(message: CreateLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerId !== 0) {
      writer.uint32(8).int64(message.ownerId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.isPublic !== false) {
      writer.uint32(32).bool(message.isPublic);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ownerId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLibraryRequest {
    return {
      ownerId: isSet(object.ownerId) ? globalThis.Number(object.ownerId) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
    };
  },

  toJSON(message: CreateLibraryRequest): unknown {
    const obj: any = {};
    if (message.ownerId !== 0) {
      obj.ownerId = Math.round(message.ownerId);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLibraryRequest>, I>>(base?: I): CreateLibraryRequest {
    return CreateLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLibraryRequest>, I>>(object: I): CreateLibraryRequest {
    const message = createBaseCreateLibraryRequest();
    message.ownerId = object.ownerId ?? 0;
    message.name = object.name ?? "";
    message.description = object.description ?? undefined;
    message.isPublic = object.isPublic ?? false;
    return message;
  },
};

function createBaseCreateLibraryResponse(): CreateLibraryResponse {
  return { libraryId: 0 };
}

export const CreateLibraryResponse: MessageFns<CreateLibraryResponse> = {
  encode(message: CreateLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.libraryId !== 0) {
      writer.uint32(8).int64(message.libraryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.libraryId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLibraryResponse {
    return { libraryId: isSet(object.libraryId) ? globalThis.Number(object.libraryId) : 0 };
  },

  toJSON(message: CreateLibraryResponse): unknown {
    const obj: any = {};
    if (message.libraryId !== 0) {
      obj.libraryId = Math.round(message.libraryId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLibraryResponse>, I>>(base?: I): CreateLibraryResponse {
    return CreateLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLibraryResponse>, I>>(object: I): CreateLibraryResponse {
    const message = createBaseCreateLibraryResponse();
    message.libraryId = object.libraryId ?? 0;
    return message;
  },
};

function createBaseUpdateLibraryRequest(): UpdateLibraryRequest {
  return { libraryId: 0, name: undefined, description: undefined, isPublic: undefined };
}

export const UpdateLibraryRequest: MessageFns<UpdateLibraryRequest> = {
  encode(message: UpdateLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.libraryId !== 0) {
      writer.uint32(8).int64(message.libraryId);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.isPublic !== undefined) {
      writer.uint32(32).bool(message.isPublic);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.libraryId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateLibraryRequest {
    return {
      libraryId: isSet(object.libraryId) ? globalThis.Number(object.libraryId) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : undefined,
    };
  },

  toJSON(message: UpdateLibraryRequest): unknown {
    const obj: any = {};
    if (message.libraryId !== 0) {
      obj.libraryId = Math.round(message.libraryId);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.isPublic !== undefined) {
      obj.isPublic = message.isPublic;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateLibraryRequest>, I>>(base?: I): UpdateLibraryRequest {
    return UpdateLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateLibraryRequest>, I>>(object: I): UpdateLibraryRequest {
    const message = createBaseUpdateLibraryRequest();
    message.libraryId = object.libraryId ?? 0;
    message.name = object.name ?? undefined;
    message.description = object.description ?? undefined;
    message.isPublic = object.isPublic ?? undefined;
    return message;
  },
};

function createBaseUpdateLibraryResponse(): UpdateLibraryResponse {
  return { success: false };
}

export const UpdateLibraryResponse: MessageFns<UpdateLibraryResponse> = {
  encode(message: UpdateLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateLibraryResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: UpdateLibraryResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateLibraryResponse>, I>>(base?: I): UpdateLibraryResponse {
    return UpdateLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateLibraryResponse>, I>>(object: I): UpdateLibraryResponse {
    const message = createBaseUpdateLibraryResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseDeleteLibraryRequest(): DeleteLibraryRequest {
  return { libraryId: 0 };
}

export const DeleteLibraryRequest: MessageFns<DeleteLibraryRequest> = {
  encode(message: DeleteLibraryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.libraryId !== 0) {
      writer.uint32(8).int64(message.libraryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteLibraryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteLibraryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.libraryId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteLibraryRequest {
    return { libraryId: isSet(object.libraryId) ? globalThis.Number(object.libraryId) : 0 };
  },

  toJSON(message: DeleteLibraryRequest): unknown {
    const obj: any = {};
    if (message.libraryId !== 0) {
      obj.libraryId = Math.round(message.libraryId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteLibraryRequest>, I>>(base?: I): DeleteLibraryRequest {
    return DeleteLibraryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteLibraryRequest>, I>>(object: I): DeleteLibraryRequest {
    const message = createBaseDeleteLibraryRequest();
    message.libraryId = object.libraryId ?? 0;
    return message;
  },
};

function createBaseDeleteLibraryResponse(): DeleteLibraryResponse {
  return { success: false };
}

export const DeleteLibraryResponse: MessageFns<DeleteLibraryResponse> = {
  encode(message: DeleteLibraryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteLibraryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteLibraryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteLibraryResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DeleteLibraryResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteLibraryResponse>, I>>(base?: I): DeleteLibraryResponse {
    return DeleteLibraryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteLibraryResponse>, I>>(object: I): DeleteLibraryResponse {
    const message = createBaseDeleteLibraryResponse();
    message.success = object.success ?? false;
    return message;
  },
};

export interface LibraryService {
  SaveArticleToLibrary(request: SaveArticleToLibraryRequest): Promise<SaveArticleToLibraryResponse>;
  GetUserLibrary(request: GetUserLibraryRequest): Promise<GetUserLibraryResponse>;
  GetLibrary(request: GetLibraryRequest): Promise<GetLibraryResponse>;
  CreateLibrary(request: CreateLibraryRequest): Promise<CreateLibraryResponse>;
  UpdateLibrary(request: UpdateLibraryRequest): Promise<UpdateLibraryResponse>;
  DeleteLibrary(request: DeleteLibraryRequest): Promise<DeleteLibraryResponse>;
}

export const LibraryServiceServiceName = "api.library.v1.LibraryService";
export class LibraryServiceClientImpl implements LibraryService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || LibraryServiceServiceName;
    this.rpc = rpc;
    this.SaveArticleToLibrary = this.SaveArticleToLibrary.bind(this);
    this.GetUserLibrary = this.GetUserLibrary.bind(this);
    this.GetLibrary = this.GetLibrary.bind(this);
    this.CreateLibrary = this.CreateLibrary.bind(this);
    this.UpdateLibrary = this.UpdateLibrary.bind(this);
    this.DeleteLibrary = this.DeleteLibrary.bind(this);
  }
  SaveArticleToLibrary(request: SaveArticleToLibraryRequest): Promise<SaveArticleToLibraryResponse> {
    const data = SaveArticleToLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SaveArticleToLibrary", data);
    return promise.then((data) => {
      try {
        return SaveArticleToLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "SaveArticleToLibrary", error));
      }
      return Promise.reject(error);
    });
  }

  GetUserLibrary(request: GetUserLibraryRequest): Promise<GetUserLibraryResponse> {
    const data = GetUserLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetUserLibrary", data);
    return promise.then((data) => {
      try {
        return GetUserLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "GetUserLibrary", error));
      }
      return Promise.reject(error);
    });
  }

  GetLibrary(request: GetLibraryRequest): Promise<GetLibraryResponse> {
    const data = GetLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetLibrary", data);
    return promise.then((data) => {
      try {
        return GetLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "GetLibrary", error));
      }
      return Promise.reject(error);
    });
  }

  CreateLibrary(request: CreateLibraryRequest): Promise<CreateLibraryResponse> {
    const data = CreateLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateLibrary", data);
    return promise.then((data) => {
      try {
        return CreateLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "CreateLibrary", error));
      }
      return Promise.reject(error);
    });
  }

  UpdateLibrary(request: UpdateLibraryRequest): Promise<UpdateLibraryResponse> {
    const data = UpdateLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateLibrary", data);
    return promise.then((data) => {
      try {
        return UpdateLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "UpdateLibrary", error));
      }
      return Promise.reject(error);
    });
  }

  DeleteLibrary(request: DeleteLibraryRequest): Promise<DeleteLibraryResponse> {
    const data = DeleteLibraryRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteLibrary", data);
    return promise.then((data) => {
      try {
        return DeleteLibraryResponse.decode(new BinaryReader(data));
      } catch (error) {
        return Promise.reject(error);
      }
    }).catch((error) => {
      if (this.rpc.handleError) {
        return Promise.reject(this.rpc.handleError(this.service, "DeleteLibrary", error));
      }
      return Promise.reject(error);
    });
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  handleError?(service: string, method: string, error: globalThis.Error): globalThis.Error;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
